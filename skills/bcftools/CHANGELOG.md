# Changelog — bcftools

## [Unreleased]

## [2026-05-25] — Initial skill
- Tool version: latest at install time (resolve path with `readlink -f "$(dirname <SKILL.md path>)/bin/bcftools"` then run `<resolved-path> --version` to check)
- Skill version: 1.0.0
- Added: initial SKILL.md, reference.md, patterns.md, environment.yaml

